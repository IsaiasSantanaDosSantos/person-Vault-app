# CLAUDE.md

## Testes automatizados

Sempre que uma alteração de código for testável, crie ou atualize o teste unitário correspondente em `lib/*.test.ts` (Vitest) na mesma alteração — não deixe para uma sessão futura. Antes de considerar a mudança pronta, rode `npm test` e confirme que passa.
